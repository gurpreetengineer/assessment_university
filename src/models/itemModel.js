// only for the reference for api's response structure

export class ItemModel {
  constructor(name, country, domains, web_pages) {
    this.name = name;
    this.country = country;
    this.domains = domains;
    this.web_pages = web_pages;
  }
}
